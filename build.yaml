name: "Jellyio Streams"
guid: "3a9f7c1e-5b6d-4e8f-9c2a-1d4b5e6f7a8b"
version: "2.0.0.3"
targetAbi: "10.11.0.0"
framework: "net9.0"
owner: "wrvines"
overview: "Stream Stremio-addon content from a self-hosted AIOStreams instance."
description: >
  On-demand: search AIOStreams from Jellyfin, add titles to the required /data/stream folder
  (TRaSH layout), and play them via signed redirects — a fresh stream is resolved on every play,
  so playback works on all clients.
category: "Metadata"
artifacts:
  - "Jellyfin.Plugin.AIOStreams/bin/Release/net9.0/Jellyfin.Plugin.AIOStreams.dll"
changelog: >
  Fix AIOStreams catalog search detection, visible setup errors, configuration saves,
  and permission handling for /data/stream.
